{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "PORT=5000 BASE_PATH='/' pnpm --filter @workspace/syntv build && rm -rf public && BUILD_OUTPUT=$(find . .. -path '*/dist/public/index.html' -print -quit | xargs dirname) && mkdir -p public && cp -R \"$BUILD_OUTPUT\"/. public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "github": {
    "silent": false
  }
}
